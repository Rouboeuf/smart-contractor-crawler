{
  "title": "Smart Contractor Crawler Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrls": {
      "title": "Start URLs",
      "type": "array",
      "description": "List of website URLs to scrape (homepage or list).",
      "editor": "stringList",
      "prefill": [
        "https://www.abgbuilds.com",
        "https://www.aboltinc.com"
      ]
    },
    "maxConcurrency": {
      "title": "Max Concurrency",
      "type": "integer",
      "description": "Number of pages to scrape in parallel.",
      "default": 10
    }
  },
  "required": ["startUrls"]
}

